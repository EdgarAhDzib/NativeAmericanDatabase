<div class="col-xs-12">
	<ul>
	{{#each entry}}
	<li> {{#each media_sources}}
		{{#if @first}}
			<img src="{{this.img_ref_1}}" width="150"/>
		{{/if}}
		{{/each}}
		<br> {{this.id}} {{{this.item_title}}} {{this.group}}
	</li>
	{{/each}}
	</ul>
</div>